# 安全政策

## 支持范围

安全边界包括浏览器端年鉴、JSON/SQL 导入、AniList 请求、Node AI 代理、部署配置和依赖供应链。

当前项目没有账号系统、服务器端年鉴数据库或跨设备同步，因此不把 `localStorage`、`sessionStorage` 或导出的备份文件视为秘密存储。

## 报告漏洞

请不要在公开 issue 中发布 API Key、可复现的真实个人数据或可直接利用的攻击细节。优先使用 GitHub 仓库的 Private vulnerability reporting / Security Advisory；如果仓库未启用该入口，请联系项目维护者后再提供最小化复现。

报告应包含：影响范围、复现步骤、受影响版本、预期与实际结果，以及不含凭证的日志或截图。我们会先确认问题，再评估修复版本和披露时间。

## 使用项目时的安全要求

- 服务端凭证只能使用 `DEEPSEEK_API_KEY` 等非 `VITE_` 变量；任何 `VITE_` 变量都会进入浏览器包。
- 生产部署必须配置明确的 `CORS_ORIGINS`，不能使用 `*`。
- JSON/SQL 备份只作为数据导入，不能复制到数据库客户端执行；SQL 导入器只解析项目固定格式。
- 不要在短评、测评输入或 Prompt 中写入密码、支付信息、身份证件或不希望发送给 AI 供应商的内容。
- 公开匿名 AI 代理应同时配置上游预算、边缘限流和异常告警；应用内存限流在多实例部署时不是全局配额。
