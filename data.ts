import { Anime } from './types';

// This file is now deprecated as we use dynamic data from Anilist.
// Keeping an empty array export to prevent immediate build errors if referenced elsewhere before full migration.
export const ANIME_DB: Anime[] = [];
